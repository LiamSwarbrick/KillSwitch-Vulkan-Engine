SDL events should be passed through to each module:
For instance, renderer needs to listen to resize events,
also this rare circumstance of SDL_EVENT_WINDOW_PIXEL_SIZE_CHANGED
when a high dpi display scaling setting changes, is another case where the swapchain will be recreated.
